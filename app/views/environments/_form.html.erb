<%= simple_form_for(@environment) do |f| %>
  <%= f.error_notification %>

  <div class="inputs">
    <%= f.input :name %>
    <%= f.input :version %>
  </div>

  <div class="actions">
    <%= f.button :submit %>
  </div>
<% end %>
